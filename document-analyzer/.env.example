# Copy to .env and adjust as needed (not currently used — config is in config/config.yaml)

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=documents

# NATS
NATS_URL=nats://localhost:4222
NATS_SUBJECT=document.analyzed

# gRPC
GRPC_PORT=50051
